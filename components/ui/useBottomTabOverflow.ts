/**
 * Hook to return the height of the bottom tab bar.
 * You can improve this logic using react-native-safe-area-context or screen size detection.
 */
export default function useBottomTabOverflow(): number {
  return 80;
}
